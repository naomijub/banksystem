{:objects
 {:Info
  {:fields
   {:id {:type String}
    :name {:type String}
    :amount {:type Float}}}}

 :queries
 {:savings
  {:type  Info
   :args {:uuid {:type (non-null String)}}

   :resolve :query/savings}}}
